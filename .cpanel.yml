---
deployment:
  tasks:
    - /bin/echo "Starting deployment..."
    - /bin/echo "Copying frontend/dist files to public_html..."
    - /bin/cp -R frontend/dist/* ~/public_html/
    - /bin/echo "Deployment complete!"

