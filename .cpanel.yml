---
deployment:
  tasks:
    - export PATH=$PATH:/usr/local/bin:/opt/cpanel/ea-nodejs18/bin
    - cd frontend
    - npm install
    - npm run build
    - cp -R dist/* ~/public_html/
    - cp .htaccess ~/public_html/.htaccess || true
    - cp public/.htaccess ~/public_html/.htaccess || true

